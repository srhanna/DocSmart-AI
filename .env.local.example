# Vercel Blob store token — copy this file to .env.local and fill in the value.
# Obtain the token from your Vercel project dashboard → Storage → doc-smart-ai-blob.
# Never commit .env.local to source control.
BLOB_READ_WRITE_TOKEN=
